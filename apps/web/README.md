# DropZone Web

Estrutura reservada para a aplicacao web do DropZone.

Hoje o Next.js ainda roda pela pasta raiz `app/`, mas os modulos foram extraidos para `features/dropzone` e `components/dropzone` para facilitar a futura migracao para `apps/web` sem mudar backend ou banco.
